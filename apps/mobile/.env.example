# Optional URL serving the seven complete frozen GeoJSON files listed in occurrenceSnapshot.ts.
# Leave unset to use the bundled authentic mobile sample.
# For a physical phone, replace localhost with the development computer's LAN IP.
EXPO_PUBLIC_OCCURRENCE_ASSET_BASE_URL=http://localhost:8765
